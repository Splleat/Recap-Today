import 'package:flutter/material.dart';

class AiFeedback extends StatelessWidget {
  const AiFeedback({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      alignment: Alignment.center,
      child: const Text('AiFeedback'),
    );
  }
}