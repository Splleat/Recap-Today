import 'package:flutter/material.dart';

class AppUsage extends StatelessWidget {
  const AppUsage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      alignment: Alignment.center,
      child: const Text('AppUsage'),
    );
  }
}